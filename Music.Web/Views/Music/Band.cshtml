@using Signum.Test.Environment

@using (var e = Html.TypeContext<BandEntity>()) 
{
	@Html.ValueLine(e, f => f.Name);
	@Html.EntityList(e, f => f.Members);
    @Html.EntityLine(e, f => f.LastAward);
    @Html.EntityList(e, f => f.OtherAwards);
}